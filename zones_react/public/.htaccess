<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /zones/
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /zones/index.html [L]
</IfModule>
